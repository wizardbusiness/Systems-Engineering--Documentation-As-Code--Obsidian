##### **Database**
A logical container within a SQL server instance that groups related objects and data in their own physical files and security context^345f48

##### **Database Objects** 
*`Schema-contained items like tables, views, stored procedures, functions that make up the structure of a database`*

##### **SQL Server Instance** 
*`A single installation of SQL Server running as a service, which can host multiple databases`*