##### *Why to install?* Lets you execute sql queries via powershell, nice for scripting or one-off queries.

```ad-pscode
collapse: true
	Install-Module -Name SqlServer -Force # [Y] to install nuget if asked
```
